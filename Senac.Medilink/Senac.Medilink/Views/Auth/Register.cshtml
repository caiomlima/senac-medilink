@* @using Senac.Medilink.Common *@
@* @model Senac.Medilink.Data.Dto.Request.Login.RegisterUserRequest *@

@* @{ *@
@*     Layout = null; *@
@* } *@

@* <form asp-action="Register" method="post"> *@
@*     <div class="form-group"> *@
@*         <label for="Email">Email</label> *@
@*         <input type="email" name="Email" required /> *@
@*     </div> *@
@*     <div class="form-group"> *@
@*         <label for="Password">Senha</label> *@
@*         <input type="password" name="Password" required /> *@
@*     </div> *@
@*     <div class="form-group"> *@
@*         <label for="UserType">Tipo de usuário</label> *@
@*         <select class="form-control" asp-for="UserType" asp-items="Html.GetEnumSelectList<UserType>()"></select> *@
@*     </div> *@
@*     <div class="form-group"> *@
@*         <label for="Name">Nome</label> *@
@*         <input type="text" name="Name" required /> *@
@*     </div> *@
@*     <div class="form-group"> *@
@*         <label for="Document">Documento</label> *@
@*         <input type="text" name="Document" required /> *@
@*     </div> *@
@*     <button type="submit">Cadastro</button> *@
@* </form> *@

@using Senac.Medilink.Common
@model Senac.Medilink.Data.Dto.Request.Login.RegisterUserRequest

@{
    ViewData["Title"] = "Medilink - Cadastro";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="~/css/login.css" rel="stylesheet">
</head>
<body>
    <div class="login-container">
        <div class="login-header">
            <img src="~/images/MediLink 5.png" alt="Logo" class="login-image">
            <h2>Cadastro</h2>
        </div>
        <form asp-action="Register" method="post">
            <div class="form-group">
                <label for="Email">E-mail</label>
                <input type="email" name="Email" id="Email" class="form-control" required />
            </div>
            <div class="form-group">
                <label for="Password">Senha</label>
                <input type="password" name="Password" id="Password" class="form-control" required />
            </div>
            @* <div class="form-group"> *@ @*Desativado pois só há cadastro de pacientes por enquanto*@
            @*     <label for="UserType">Tipo de Usuário</label> *@
            @*     <select class="form-control" asp-for="UserType" asp-items="Html.GetEnumSelectList<UserType>()"></select> *@
            @* </div> *@
            <div class="form-group">
                <label for="Name">Nome</label>
                <input type="text" name="Name" id="Name" class="form-control" required />
            </div>
            <div class="form-group">
                <label for="Document">Documento (CPF)</label>
                <input type="text" name="Document" id="Document" class="form-control" required />
            </div>
            <div class="d-grid">
                <button type="submit" class="btn btn-primary">Cadastrar</button>
            </div>
        </form>
        <div class="login-footer">
            <p>Já possui uma conta? <a href="/Auth/Index">Fazer login</a></p>
        </div>
    </div>
</body>
</html>
